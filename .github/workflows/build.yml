name: Build Physarum

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Pobierz projekt
      uses: actions/checkout@v4

    - name: Pobierz openFrameworks
      run: |
        git clone --depth 1 https://github.com/openframeworks/openFrameworks.git

    - name: Pobierz ofxGamepad
      run: |
        git clone https://github.com/Bleuje/ofxGamepad.git openFrameworks/addons/ofxGamepad

    - name: Skopiuj projekt
      shell: pwsh
      run: |
        New-Item -ItemType Directory -Force -Path openFrameworks/apps/myApps/interactive-physarum
        Get-ChildItem -Exclude ".git",".github" | Copy-Item -Destination openFrameworks/apps/myApps/interactive-physarum -Recurse -Force

    - name: Budowanie
      run: |
        cd openFrameworks/apps/myApps/interactive-physarum
        make Release

    - name: Spakuj wynik
      uses: actions/upload-artifact@v4
      with:
        name: interactive-physarum-build
        path: openFrameworks/apps/myApps/interactive-physarum/bin
